<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spindle02">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".gapData"
            android:label="@string/gap" />
        <activity
            android:name=".gapyData"
            android:label="@string/proximity" />
        <activity
            android:name=".rpmData"
            android:label="@string/rpm" />
        <activity
            android:name=".Graph_gapx"
            android:label="@string/gap" />
        <activity
            android:name=".Graph_gapy"
            android:label="@string/proximity" />
        <activity
            android:name=".Graph_rpm"
            android:label="@string/rpm" />
        <activity android:name=".DataActivity" />
        <activity
            android:name=".GraphActivity"
            android:label="@string/temp" />
        <activity android:name=".DataListAdapter" />
        <activity
            android:name=".Management"
            android:label="@string/temp" />
        <activity android:name=".LoginRequest" />
        <activity android:name=".ValidateRequest" />
        <activity android:name=".RegisterRequest" />
        <activity
            android:name=".SignActivity"
            android:label="@string/signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>